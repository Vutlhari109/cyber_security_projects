# 🔍 Cybersecurity Scanner API (Nmap + Whois + Nslookup)

This project is a basic cybersecurity tool built using Flask and Python. It allows users to send a `POST` request with a domain name and returns information from:

- `nmap` (basic port scan)
- `whois` (domain registry info)
- `nslookup` (DNS resolution)

---

## 🚀 How to Run

```bash
pip install flask
python app.py